// Command ratchet is the single binary. It currently does nothing: iteration 1
// exists to put a module, a Makefile and a working gate on disk, so that
// closing it is the first proof the gate can run at all.
package main

import "fmt"

func main() {
	fmt.Println("ratchet")
}
